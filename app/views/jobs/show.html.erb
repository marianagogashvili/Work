<%= @job.title %>
<%= @job.employer.name %>
<%= @job.vacant %>
</br>
<%= session[:employer_id] %>

<% if check? == false %>
  <%= button_to "apply", apply_path %>
<% else %>
  <%= button_to "apply", apply_path, :disabled => true %>
<% end %>

